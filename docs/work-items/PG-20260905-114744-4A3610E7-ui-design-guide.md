# PropertyGoJB UI audit and proposed agent guide

## Identity and ownership

- **ID:** PG-20260905-114744-4A3610E7-ui-design-guide
- **Status:** IMPLEMENTED (Phases 1–7 complete) under the recorded owner authorization below. Automated verification (typecheck, lint, tests, build, SDLC registry/calculator checks) passes. Browser-based accessibility/responsive/reduced-motion verification and an independent code review were subsequently performed and found real defects, which were fixed and re-verified — see "Independent review + defect fixes" near the end of this file. Full fresh-session agent-discovery testing and final human sign-off remain pending.
- **Human owner / requested by:** Repository owner (current user).
- **Date:** 5 September 2026 (initial audit); continued 5 September 2026 (research + concrete phased proposal, this update).
- **Original request:** "Now I want a complete guide for copilot/codex to generate UI always follow rules, can you please analyze through my current design, and any improvements needed based on your analyze."
- **Continuation request:** Audit components/tokens/dependencies, research current design trends once (save dated sources for reuse), select a cohesive modern tech-focused direction, enhance/create reusable components with Lucide icons, apply DRY tokens/typed props/variants, choose an animation approach with reduced-motion support, cover accessibility/responsive/state coverage, and update this work item with a concrete proposal, exact affected files, phased tasks, and verification criteria — requesting R3 approval before activating agent instructions or implementing protected changes.
- **Risk / autonomy:** R3 for agent-instruction activation (unchanged). The Phase 1–5 implementation below is calculated R2/A2 (bounded, reversible, outside protected domains) by `npm run sdlc:score`, but is held for explicit human approval before this session implements it, because it is one coordinated identity change across many shared, widely-reused files — see "Risk, delivery, and data controls" below.
- **Source snapshot:** `9498d8b`, branch `external-website`, plus existing working-tree files, re-verified against current file contents during this update (package.json, components.json, globals.css, common/ui components). Source analysis only; no browser screenshots, runtime interaction, contrast measurements, or performance measurements collected.
- **Required approver:** Human repository owner for the exact guide/instruction additions and for the phased implementation proposal below. Independent review remains pending; this document is not self-approval.

## Outcome, scope, and acceptance

Make future UI work consistent with PropertyGoJB's existing identity, with explicit rules, reusable references, and verifiable acceptance checks. Instruction files guide generation; tests and human review catch deviations. No instruction file can guarantee every generated result complies.

| ID               | Requirement                              | Acceptance and verification                                                                                                                                                  |
| ---------------- | ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| REQ-001 / AC-001 | Analyze current design                   | Source-backed findings distinguish existing conventions, defects, and proposed refinements. Reference review.                                                                |
| REQ-002 / AC-002 | Prepare a complete UI generation guide   | Covers tokens, typography, layout, components, forms, states, navigation, accessibility, content, responsive behavior, performance, and workflow. Section-by-section review. |
| REQ-003 / AC-003 | Make it discoverable by both agents      | After approval, one guide with short entry points in Codex and Copilot instruction files; new-session discovery checks for both tools. Pending activation.                   |
| REQ-004 / AC-004 | Preserve current work and business rules | No application, API, schema, dependency, workflow, registry, or existing dirty-file edits in this planning phase. Scope review.                                              |

All requirements are Must, sourced from the user and current repository controls. Affected users: anonymous visitors, CUSTOMER, AGENT, ADMIN, and SUPER_ADMIN. Surfaces: public discovery, authentication, customer account, agent/admin operations. APIs, tables, jobs, integrations, permissions, and production behavior are unchanged.

**Non-goals:** Implement the improvement backlog, redesign the brand, add a component dependency or browser-test framework, activate learned rules, alter business transitions, merge, or deploy. Approval of this guide does not approve those changes.

**Assumptions:** Retain the blue/slate identity and Inter; improve consistency incrementally. Support the app's existing theme capability. Suggested scales below are design proposals, not claims of an already enforced system.

## Current-design assessment

The visual language is recognizable: blue calls to action, slate text, spacious rounded panels, bold headings, photographic project cards, and a dark public hero. Public pages are more theme-aware than internal and authentication screens. The largest opportunity is consistent behavior and shared styling, rather than a replacement visual identity.

### Strengths to preserve

- [Global CSS](../../src/app/globals.css) provides semantic colors and light/dark values; [components.json](../../components.json) establishes Tailwind CSS variables, Radix Nova, and Lucide.
- [Root layout](../../src/app/layout.tsx) loads Inter and central theme, toast, and confirmation providers.
- [Public shell](../../src/components/public/public-shell.tsx) uses a shared maximum width, responsive navigation, semantic surface colors, and a Radix-backed mobile Sheet.
- [Project card](../../src/components/public/public-project-card.tsx) keeps image, price, project name, location, and availability in a reusable component with missing-data fallbacks.
- [AppReveal](../../src/components/common/app-reveal.tsx) already respects reduced motion; [enquiry form](../../src/components/public/public-enquiry-form.tsx) correctly explains that a requested viewing awaits confirmation.
- Existing common components and domain views give future agents references to reuse instead of inventing another UI system.

### Prioritized improvement backlog

Priorities indicate recommended implementation order, not a security severity rating. Each future implementation must be scored separately.

| Priority | Finding and source evidence                                                                                                                                                                                                                                                                                                            | Recommended change                                                                                                                                                           | Acceptance evidence                                                                                                                    |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| P1       | The [internal shell](../../src/components/internal/shell/internal-shell.tsx) mobile overlay uses custom divs and buttons without dialog semantics, focus containment, Escape handling, or focus restoration. Its sidebar is fixed at 290px inside a narrower small-screen panel.                                                       | Compose the existing Sheet primitive, preserve role navigation, make sidebar width fit its container.                                                                        | Keyboard open/close, Tab/Shift+Tab containment, Escape, restored focus, 320px viewport, no clipped controls.                           |
| P1       | [Confirmation provider](../../src/components/common/app-confirm-provider.tsx) handles Escape but renders a custom overlay; [confirmation button](../../src/components/common/app-confirm-button.tsx) duplicates confirmation presentation.                                                                                             | Consolidate presentation around the existing Dialog primitive while preserving public caller APIs and confirmation semantics.                                                | Named dialog, safe initial focus, focus restoration, correct cancel/confirm behavior, no accidental duplicate mutation.                |
| P1       | [Internal shell](../../src/components/internal/shell/internal-shell.tsx), [auth shell](../../src/components/auth/auth-page-shell.tsx), [AppButton](../../src/components/common/app-button.tsx), and [status badge](../../src/components/common/app-status-badge.tsx) hard-code light surface colors despite the global theme provider. | Move general surfaces to semantic tokens; add reviewed brand/status pairs for both themes. Do not blindly replace intentional image overlays or dark hero colors.            | Light/dark screenshots and measured contrast for text, borders needed to identify controls, focus, hover, selected, and error states.  |
| P1       | [Enquiry form](../../src/components/public/public-enquiry-form.tsx) sends project/phone validation errors to transient toasts; [toast provider](../../src/components/common/app-toast-provider.tsx) has no explicit live-region semantics.                                                                                             | Add persistent field errors and a linked summary where appropriate, focus the first invalid field, and make feedback accessible. Keep toast supplementary.                   | Invalid mobile/project and server failure remain visible, associated with the field, and announced; entered data survives failure.     |
| P1       | Public/internal navigation has visual active styles without `aria-current`; shells do not provide a skip link. Public mobile links have an X icon and are not wrapped in SheetClose.                                                                                                                                                   | Add current-page semantics and skip-to-content behavior. Close the public Sheet on selection; replace the misleading X on navigation items.                                  | Keyboard bypass works; current item is announced; link selection closes the menu. Public menu persistence needs runtime confirmation.  |
| P2       | [Admin leads](<../../src/app/(internal)/admin/leads/page.tsx>) nests `p-8` sections and uses a `min-w-[1280px]` table. Scrolling exists, but key actions may be distant on mobile.                                                                                                                                                     | Reduce small-screen padding; provide compact mobile rows/cards or an explicitly labeled, keyboard-accessible horizontal table region.                                        | Essential identity/status/action visible at 320/390px; all columns/actions remain reachable; no page-wide overflow.                    |
| P2       | [AppButton](../../src/components/common/app-button.tsx) and project cards translate/scale on hover without a reduced-motion alternative; toast uses spring/drag motion without a reduced-motion branch.                                                                                                                                | Apply reduced-motion behavior consistently to CSS and Motion interactions. Retain explicit toast dismissal.                                                                  | Reduced-motion mode removes decorative movement and preserves content and actions.                                                     |
| P2       | [Project card](../../src/components/public/public-project-card.tsx) uses `Image unoptimized` and a broad `sizes` value. This is a delivery choice, not proof of slow performance.                                                                                                                                                      | Review media-host constraints and actual rendered sizes before enabling optimization; use a narrow host configuration if a separate approved configuration change is needed. | Compare image bytes, visual quality, and LCP in an isolated preview; broken-image fallback remains usable.                             |
| P2       | No `loading.*`, `error.*`, or `not-found.*` files were found under `src/app`. Component-local states exist.                                                                                                                                                                                                                            | Add boundaries where asynchronous routes need them, with route-specific fallback, retry, and not-found experiences.                                                          | Slow response, failure, and missing record scenarios verified; no fabricated empty results on failures.                                |
| P2       | [Global CSS](../../src/app/globals.css) maps `--font-sans` to itself and mono to `--font-geist-mono`; [root layout](../../src/app/layout.tsx) applies Inter's class rather than its variable.                                                                                                                                          | Wire a deliberate font variable mapping and verify computed font styles. Inter body usage already exists; this is token wiring cleanup.                                      | Body, `font-sans`, headings, and mono use intended computed fonts with no missing variable.                                            |
| P3       | Large radii, custom shadows, blue accents, and heavy headings recur as page-local classes; primitive Button is neutral while AppButton is blue.                                                                                                                                                                                        | Define semantic brand tokens and shared panel/header variants; reduce excessive font-black/uppercase use in dense operations screens.                                        | Representative public/internal screens retain identity with fewer one-off styles; primary/destructive action meanings remain distinct. |

Some session-memory statements are older than the workspace: `.github/workflows/ci.yml` and `.github/CODEOWNERS` are currently present as pre-existing untracked work. Their existence does not prove remote CI or branch enforcement. No existing baseline failures were re-certified during this source audit.

## Proposed complete UI guide

**Publication destination after approval:** `docs/UI_DESIGN_GUIDE.md`. This entire section is a draft specification, not active agent policy. When publishing, retain the audit as delivery history here and copy the guide with repository links rebased from `../../src/` to `../src/`.

### 1. Authority and working method

Follow applicable repository instructions, the SDLC skill, active business rules, and the approved work-item scope. Read version-matched local Next.js guides before framework changes. This guide defines visual and interaction conventions; it does not authorize changes to permissions, publication, booking, payment, document decisions, or consent.

For every UI task:

1. Identify the persona, route, primary user task, data source, and closest existing screen. Inspect its shell and imported components.
2. Inspect `src/app/globals.css`, `components.json`, and relevant `src/components/ui`, `common`, and domain components. Read actual prop types before using them.
3. State a short implementation contract: files, reused components, layout, data/interaction states, responsive behavior, and acceptance checks. Infer routine choices.
4. Run request-only and exact-file SDLC scoring before mutation. Read matched rules and observe their gates.
5. Implement the smallest scoped change. Record deficiencies in existing shared components instead of silently copying them or refactoring unrelated screens.
6. Verify behavior and visual states; record actual results and unverified cases. Update the guide only when an approved design decision changes it.

### 2. Product identity and surface recipes

Use the current blue/slate palette, Inter typography, Lucide icons, rounded panels, restrained shadows, and real project media. Keep decorative gradients primarily in brand/hero areas. One dominant action per task region is the default; avoid competing filled buttons.

| Surface          | Composition                                                                                                 | Reference                                                                                      |
| ---------------- | ----------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Public landing   | Shared shell; dark hero with clear value proposition and search; project discovery; enquiry support         | `src/app/(public)/page.tsx`, `src/components/public/public-shell.tsx`                          |
| Public listing   | Heading, URL-aware filters where established, results/count, cards and map alternative, no-results recovery | `src/app/(public)/projects/page.tsx`, `public-projects-filters.tsx`, `public-project-card.tsx` |
| Project detail   | Title/location, media, truthful price and availability, layouts/details, enquiry or viewing action          | `src/app/(public)/projects/[slug]/page.tsx`                                                    |
| Customer account | Account shell, journey/status summary, scoped records and clear next action                                 | `src/app/(public)/account/layout.tsx`                                                          |
| Authentication   | Existing AuthPageShell/AuthCard; focused form; optional large-screen brand panel                            | `src/components/auth/auth-page-shell.tsx`, `auth-card.tsx`                                     |
| Internal list    | Existing role shell; compact heading/actions; useful summary; filters; table/mobile equivalent              | `src/app/(internal)/admin/leads/page.tsx`                                                      |
| Internal detail  | Identity and status first; grouped facts; history; clearly separated authorized actions                     | `src/components/internal/bookings/booking-detail-view.tsx`                                     |

Do not add another shell inside a route already wrapped by one. Preserve one primary main landmark and one descriptive h1 per page. References demonstrate structure; the audit lists defects that must not become templates.

### 3. Colors and tokens

Use existing semantic classes for ordinary UI:

| Purpose                | Current classes                                                  |
| ---------------------- | ---------------------------------------------------------------- |
| Page                   | `bg-background text-foreground`                                  |
| Panel/card             | `bg-card text-card-foreground border-border`                     |
| Secondary surface/text | `bg-muted text-muted-foreground`                                 |
| Overlay content        | `bg-popover text-popover-foreground`                             |
| Focus/input            | `border-input`, visible `ring-ring` treatment                    |
| Destructive action     | Existing Button `variant="destructive"` and destructive feedback |

Current `primary` is neutral; it is not the blue brand color. Until a reviewed token migration, reuse AppButton for established public brand actions. Do not globally recolor `primary` to blue without reviewing all consumers.

Proposed future tokens: brand/background-on-brand, brand-subtle/foreground, and status success/warning/info foreground-background-border sets. These tokens do not exist yet. Do not generate classes using them until implemented. Avoid fresh hex values, arbitrary shadows, or page-specific palettes unless an approved media/chart requirement needs them. Document intentional fixed-color hero/image overlays.

### 4. Typography, spacing, and shape

These are proposed defaults derived from current screens; match a sound neighboring pattern when it has a specific need.

| Role                | Default                                                                       |
| ------------------- | ----------------------------------------------------------------------------- |
| Public hero title   | `text-4xl sm:text-5xl lg:text-6xl`, tight tracking; bold/black sparingly      |
| Page title          | `text-2xl sm:text-3xl font-bold tracking-tight`                               |
| Section title       | `text-xl sm:text-2xl font-semibold`                                           |
| Body                | Public `text-base leading-7`; compact internal `text-sm leading-6`            |
| Label               | `text-sm font-medium` or semibold                                             |
| Supporting metadata | `text-xs` only for genuinely secondary content; readable contrast             |
| Public container    | `mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8`                               |
| Internal page inset | `p-4 sm:p-6 lg:p-8`; avoid stacked large mobile insets                        |
| Spacing             | Prefer Tailwind steps 1, 2, 3, 4, 6, 8, 12, 16; consistent gaps within groups |
| Field/button radius | `rounded-xl` or existing `rounded-2xl` public form treatment                  |
| Panel radius        | `rounded-2xl`; preserve existing 2rem public feature cards where appropriate  |
| Badge radius        | `rounded-full`                                                                |

Prefer sentence case for labels and explanatory text. Reserve uppercase tracking for short eyebrows, not paragraphs. Use consistent number alignment in numeric columns. Do not truncate critical prices, statuses, or action labels. Long project names must remain available through wrapping or the detail page, not hover alone.

### 5. Component selection and composition

| Need                               | Reuse first                                            | Important limit                                                                            |
| ---------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------ |
| Public brand CTA                   | `common/app-button.tsx`                                | `appVariant`: primary, soft, outline; inspect theme/motion limitations                     |
| Compact, ghost, destructive action | `ui/button.tsx`                                        | Real variants/sizes only; AppButton's brand styling can conflict with destructive variants |
| Input / textarea / label           | `ui/input.tsx`, `ui/textarea.tsx`, `ui/label.tsx`      | Preserve accessible IDs, error linkage, and appropriate input attributes                   |
| Search/select                      | `common/app-search-input.tsx`, `common/app-select.tsx` | AppSelect uses `ariaLabelledBy`; do not assume arbitrary ARIA props are forwarded          |
| Status                             | `common/app-status-badge.tsx`                          | Tone is not the domain status; pass meaningful visible text; theme refinement is pending   |
| Dialog / mobile drawer             | `ui/dialog.tsx`, `ui/sheet.tsx`                        | Use their title/description and focus behavior; do not hand-build a modal overlay          |
| Tabs, menus, table                 | Existing `ui` primitives                               | Preserve semantic markup and keyboard behavior                                             |
| Toast/confirmation                 | Existing app feedback APIs                             | Do not introduce a second global system; fix shared accessibility within approved scope    |
| Project card / inventory / booking | Existing public or internal domain component           | Preserve data contract, units, status, and authorized actions                              |
| Reveal                             | `common/app-reveal.tsx`                                | Decorative only; important content must remain available without animation                 |

Use `cn` for class composition. Extend a shared variant when multiple callers need the same behavior; do not create another button/card framework. Native buttons are appropriate for specialized triggers when their semantics, focus, states, and dimensions are complete. Use Link for navigation and button for actions. `asChild` must wrap a suitable single element; never nest interactive elements inside a linked project card.

### 6. Responsive layout and navigation

Build from the narrow layout upward. Verify at 320, 390, 768, 1024, and 1440 CSS pixels as a project test matrix, plus content zoom. Grids stack naturally; use `min-w-0`, wrapping, and bounded media where needed. Do not hide overflow globally to conceal layout defects.

Keep existing public navigation at the `lg` transition and the internal desktop sidebar pattern unless the task explicitly changes navigation. On small screens, use Sheet with an accessible trigger, title, close control, Escape, focus containment/restoration, scrollable content, and closure on route selection. Mark the active link with `aria-current="page"`. Provide a visible-on-focus skip link to the main content target.

For wide operational tables, choose a mobile card/row view or an intentional scroll region with a visible cue and keyboard access. Retain table headers and accessible names. Keep key record identity and actions reachable; preserve filters, pagination, sorting, and query parameters where the existing route supports them. Do not invent unsupported server sorting or counts.

### 7. Forms and feedback

Every field has a persistent visible label. Show required/optional information in text or semantics, not color alone. Use suitable types, autocomplete, inputMode, and unit hints. Preserve raw values on failure. Validation on the client improves feedback; server validation remains authoritative.

Associate errors with the field through `aria-describedby` and `aria-invalid`; give IDs to hints and error messages. Custom components that cannot forward these need a scoped API extension, not imaginary props. For multi-field failures, provide an error summary with links and move focus appropriately. Do not rely solely on a disappearing toast.

During submission, show a pending label and prevent duplicate submission. Do not claim success until the server confirms. Preserve retryable input after network/server errors. Explain unavailable actions. Use appToast for supplementary feedback; require an accessible persistent status where needed. Confirmation copy names the action and affected record and provides a safe cancellation path. UI confirmations never replace server authorization.

### 8. Required state matrix

Each applicable state must be designed and tested, or explicitly marked N/A with a reason.

| State                       | Expected presentation                                                                    |
| --------------------------- | ---------------------------------------------------------------------------------------- |
| Loading                     | Stable layout or skeleton; named progress where needed; no fake data or arbitrary delays |
| Empty dataset               | Explain absence and offer a valid next step                                              |
| Filter has no matches       | Retain filters; show reset/change-filter action                                          |
| Validation failure          | Persistent specific error, field association, preserved values                           |
| Network/server failure      | Safe message and retry; no raw stack trace or success-looking empty list                 |
| Pending mutation            | Clear pending action, duplicates prevented                                               |
| Success                     | Confirm actual outcome and meaningful next action                                        |
| Disabled/unavailable        | Explain why and how to proceed if possible                                               |
| Unauthorized/missing record | Respect server response and privacy; do not expose hidden record details                 |
| Partial/missing media       | Stable fallback with truthful labels; usable project navigation                          |
| Stale/conflicting record    | Explain refresh/retry when server rejects an outdated action; never force success        |

### 9. Accessibility and motion

Target WCAG 2.2 AA as a design/verification goal, not a certification claim. Normal text should reach 4.5:1 contrast; large text 3:1; applicable non-text UI indicators 3:1. Verify real combinations, including overlays and both themes. WCAG's 24px minimum pointer target has exceptions; this project's proposed touch-friendly default is at least 44px for primary/mobile controls. Dense controls require deliberate spacing and verification. See the [W3C quick reference](https://www.w3.org/WAI/WCAG22/quickref/).

Use semantic headings, landmarks, lists, buttons, and table markup. Name icon-only controls; decorative icons should not repeat spoken text. Do not convey a status only through color. Keep focus visible and unobscured by sticky UI. Verify keyboard operation, screen-reader announcements, 200% text zoom, and reflow. Maps and charts need meaningful textual/list alternatives.

Respect reduced motion for both Motion components and CSS transforms/transitions. Remove decorative translation, scaling, spring effects, and long reveals under that preference. Keep content visible and controls usable. Use explicit dismissal controls, never drag-only interaction. Avoid animation for critical status comprehension.

### 10. Property content and domain truth

Show prices and units exactly as supported by existing formatters/contracts. Use MYR/RM consistently with the surface; identify monthly estimates as estimates and reuse existing calculator assumptions. Do not imply financing approval. Use Malaysia time and server-supported viewing slots. A viewing request is not a confirmed appointment.

Render published project information from the approved data path. Never invent a project price, availability count, discount, testimonial, agent identity, or promotional urgency to fill a layout. Distinguish unknown from zero. Preserve booking/payment/document status wording and server-authorized transitions. Hidden/disabled buttons are not authorization.

Consent remains required before marketing tracking. Use existing contact/link builders; creating a UI must not send customer messages. Engineering fixtures and screenshots use synthetic data and public assets only; no customer records, credentials, OTPs, or uploaded documents in agent context.

### 11. Framework, images, and performance

Keep server-rendered route composition and data access on the server; isolate interactive state/browser APIs in small Client Components. Send minimal serializable props across the boundary. Follow the installed Next.js guides for async route parameters, images, metadata, and route boundaries rather than older conventions.

Use Next Image with dimensions or a sized `fill` container, meaningful alt text, and accurate responsive `sizes`. Decorative images have empty alt text. Preserve deliberate `unoptimized` exceptions until their media constraints are understood. Do not preload every card image or add external hosts/packages casually. Measure the actual hero/LCP candidate. Avoid unnecessary client libraries and blocking animations.

Keep dynamic maps browser-bound as in the current implementation; maintain a useful project list and loading fallback. Charts require meaningful labels and non-color differentiation. Record performance comparisons when media, large lists, maps, or client boundaries change; do not invent benchmark results.

### 12. Completion checklist and evidence

- Confirm the declared persona, primary action, reference screen, reused components, and exact file manifest.
- Confirm theme tokens, readable text, spacing, responsive layout, no clipped actions, and appropriate visual hierarchy.
- Exercise the applicable state matrix, keyboard navigation, dialog focus, errors, labels, reduced motion, and both themes.
- Use synthetic screenshots for representative changed routes at mobile and desktop widths, plus problematic breakpoints and long-content states. Record route, viewport, theme, data state, and observation.
- Run calculator-listed checks. For source changes follow the repository's clean-CI matrix: installation, route type generation, formatting, lint, typecheck, tests, build, dependency/security checks, and relevant integration/accessibility checks. Separate inherited failures from introduced failures; a scoped pass does not prove full CI passes.
- Add meaningful behavioral tests for changed interaction contracts. Do not write tests that merely repeat class strings. Use browser/visual checks where layout is the actual concern.
- Provide evidence and a rollback procedure. Mark missing environments/checks as unverified, not passed. Obtain required independent/human review.

### 13. Ready-to-use task prompt

```text
Implement [UI task] for [persona] on [route].
Follow the repository SDLC and read docs/UI_DESIGN_GUIDE.md before editing.
Inspect the closest existing screen, its shell, tokens, and shared components.
Use [synthetic fixture or existing approved data contract].
Preserve domain rules and existing business behavior outside the request.
First state the scoped files, reused components, primary action, state matrix,
and responsive/accessibility checks. Apply the risk gate before mutation.
Implement within the authorized scope, verify the result, and report evidence,
unverified cases, and rollback. Do not invent component APIs or business data.
```

### 14. Maintenance and exceptions

Owner: PropertyGoJB product/engineering owner. Review when shared primitives, theme tokens, or major layout patterns change. An exception records the affected rule, user need, evidence, scope, owner, and validation. It is not permission to bypass SDLC or active business rules. Keep proven examples fresh; do not silently turn a proposed backlog item into current policy or a component that agents may assume exists.

## Exact proposed agent activation

After approval, publish the guide above and append the following section to both `AGENTS.md` and `.github/copilot-instructions.md`, preserving all current content:

```markdown
## UI generation

For UI creation or changes, read `docs/UI_DESIGN_GUIDE.md` before editing.
Inspect the closest existing screen, its shell, `src/app/globals.css`, and relevant
shared components. Reuse actual component APIs and semantic theme tokens.
Cover responsive layouts, keyboard/focus behavior, labels/errors, both themes,
reduced motion, and applicable loading/empty/error/pending/success states.
Preserve domain rules and report verification evidence and unverified cases.
This guide supplements the SDLC and does not change its approval gates.
```

Create `.github/instructions/ui.instructions.md` with this exact proposed content:

```markdown
---
applyTo: "src/**/*.tsx,src/**/*.css"
---

# PropertyGoJB UI

Read `docs/UI_DESIGN_GUIDE.md` before UI implementation. Reuse the applicable
public/auth/account/internal shell and existing shared/domain components.
Read component prop types before use. Match existing semantic theme tokens;
do not invent tokens, component APIs, business data, or status transitions.
Define applicable loading, empty, error, pending, success, and unavailable states.
Verify mobile/desktop layouts, both themes, keyboard/focus behavior, accessible
names and errors, and reduced motion. Report evidence and unverified cases.
Follow the repository SDLC and active business rules; this file grants no
additional permission and does not bypass protected-domain instructions.
```

The root entry points cover UI-affecting `.ts` helpers and configuration as well; the path-specific file intentionally focuses on TSX/CSS without route-group glob ambiguity.

Codex discovers `AGENTS.md` instructions when a run/session starts, and nested/global overrides can affect them. Keep the entry point short and explicitly require reading the detailed guide; merely creating a guide does not make it automatically loaded. Verify in a fresh session by asking it to list the instruction sources and UI conventions it read. See [official Codex instruction guidance](https://learn.chatgpt.com/docs/agent-configuration/agents-md).

Copilot supports repository instructions and, on supported surfaces, path-specific instructions with `applyTo`; support varies by product. Verify the target editor/agent shows the instruction source, then give it a harmless UI analysis task and inspect whether it reads the guide. See [GitHub setup](https://docs.github.com/en/copilot/how-tos/copilot-on-github/customize-copilot/add-custom-instructions/add-repository-instructions) and [support matrix](https://docs.github.com/en/copilot/reference/custom-instructions-support). This verification is pending, not claimed completed.

## Risk, delivery, and data controls

| Calculator pass               | Manifest                                                                                                                                                          | Score / result                                                                                                                           |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Request only                  | Analyze current UI design and prepare a complete Copilot and Codex UI generation guide with evidence-based improvements                                           | 0 / R0 informational; BR-UI-001 context; source review and human acceptance                                                              |
| Proposed activation           | This work item, `docs/UI_DESIGN_GUIDE.md`, `AGENTS.md`, `.github/copilot-instructions.md`, `.github/instructions/ui.instructions.md`                              | 60 / R3; BR-CTRL-001 policy, BR-UI-001 partial; human plan approval before implementation                                                |
| Final planning manifest       | Only this work item                                                                                                                                               | 30 / R2 planning-only score; overall R3 activation gate retained; no risk downgrade                                                      |
| Continuation request only     | Audit + trend research + phased concrete UI proposal for this work item                                                                                           | R2 (Moderate); BR-UI-001, BR-TEST-001 matched; verified via `npm run sdlc:score -- --request "…"` this session                           |
| Continuation Phase 1 manifest | `src/app/globals.css`, `app-button.tsx`, `app-status-badge.tsx`, new `app-icon-button.tsx`/`app-empty-state.tsx`/`app-error-state.tsx`/`app-loading-skeleton.tsx` | R2/A2 (Moderate); BR-UI-001, BR-NEXT-001 matched; verified via `npm run sdlc:score -- --files "…"` this session; held for human approval |

Full matched registry entries were read. Required controls: human plan approval, control-plane tests, scope review, independent review, reuse primitives, keyboard/screen-reader/responsive/reduced-motion checks. Required gates also include applicable security/privacy review, integration/E2E and staging evidence, and rollback planning.

**Delivery:** After approval, publish only the guide and exact three instruction additions, rebase source links, record approval here, re-score all five files, validate formatting/links/registry/calculator, and verify instruction discovery. Defer application improvements to separately scoped work items. No registry promotion, dependencies, or CI changes are included.

**Ownership:** Main assistant owns this new planning file. Existing `AGENTS.md`, `README.md`, `package.json`, `.github/`, `DESIGN.md`, SDLC docs/scripts, and WhatsApp work are preserved. No unrelated cleanup. Future activation must reread the dirty instruction files and append without replacing their contents.

**Rollback:** Before activation, remove only this new record if rejected. After activation, reverse only the guide addition and the exact instruction additions from the reviewed diff, preserving pre-existing work. No database rollback or deployment is involved.

## Continuation (5 September 2026): dated trend research, saved for reuse

Captured once; a copy is also kept in coding-agent repository memory (`ui-design-trends-2026-09.md`) so later tasks reuse it instead of re-researching. Re-fetch a source only if a decision below is revisited.

| Source (dated)                                                                                          | What it shows                                                                                                                                                                                                                                                             |
| ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [motion.dev](https://motion.dev/) — changelog to 18 Aug 2026 (v13.1.1); Motion UI announced 23 Jul 2026 | MIT-licensed, React-first hooks (`useReducedMotion`, `AnimatePresence`, layout/scroll/gesture animation), "up to 90% smaller than GSAP", agent-oriented docs. Already a project dependency at `motion@^12.40.0`.                                                          |
| [animejs.com](https://animejs.com/) — v4                                                                | Imperative JS animation engine, ~24.5 KB core, modular/tree-shakable; Scroll Observer, Timeline, Draggable, SVG morphing. No built-in React reduced-motion hook; would need manual wiring.                                                                                |
| [m3.material.io](https://m3.material.io/) — "M3 Expressive" (Google I/O 2025/2026)                      | Industry direction toward emotion-driven but purposeful motion, adaptive components, expanded shape/typography — supports "purposeful motion, not decoration" rather than a specific palette.                                                                             |
| [lucide.dev](https://lucide.dev/) — v1.41.0 latest                                                      | Confirms Lucide is tree-shakable, customizable (stroke/size), and matches `components.json`'s configured `iconLibrary: "lucide"`. Repo's `lucide-react` is `^1.18.0` — behind latest, upgrade is a separate `package.json` (protected-domain) decision, not bundled here. |

**Decisions carried forward (do not re-litigate without new evidence):**

1. **Animation library: keep `motion`; do not add Anime.js.** Motion already provides spring/gesture/scroll/stagger with a first-class React reduced-motion hook; Anime.js would add a second animation runtime and bundle cost for capabilities Motion already covers in this app (no SVG-morph/motion-path requirement exists today).
2. **Icons: Lucide only**, consistent with existing usage and `components.json`. No second icon set. Version bump is out of scope for this UI initiative.
3. **Design direction:** "modern, tech-focused" here means _purposeful_ motion (state-communicating, not decorative-only), token-driven brand color instead of hardcoded hex/blue-6xx utility classes, one consistent radius/shape scale, consistent Lucide icon sizing, and real loading/empty/error states — not a framework change. Tailwind v4 + shadcn `radix-nova` + CVA remain the foundation.

## Continuation (5 September 2026): concrete component and token proposal

This section makes the Priority-1/2/3 backlog above (see "Current-design assessment") actionable with exact files, and adds the DRY/token/variant/animation/state mechanics the continuation request asked for. Nothing in this section is implemented; it is the concrete plan awaiting approval described under "Risk, delivery, and data controls" (updated below).

### A. Shared tokens (`src/app/globals.css`)

Add semantic pairs inside the existing `:root`/`.dark`/`@theme inline` blocks, alongside (not replacing) current shadcn tokens:

- `--brand`, `--brand-foreground`, `--brand-subtle`, `--brand-subtle-foreground` (light/dark values derived from the current blue-600/700 hand-picked colors already used in `AppButton` and the public hero, expressed as reviewable `oklch()` values consistent with the file's existing color format).
- `--success`, `--warning`, `--info` each with a `-foreground` and implicit border via existing `color-mix` pattern already used for `secondary` hover in `src/components/ui/button.tsx`.
- Corresponding `--color-*` mappings in the `@theme inline` block so Tailwind utilities (`bg-brand`, `text-brand-foreground`, `bg-success`, …) become available project-wide, the same mechanism already used for `--color-primary` etc.

No existing token is renamed or removed, so current usages of `bg-primary`, `bg-secondary`, `bg-destructive`, etc. keep working unchanged.

### B. Component enhancements (existing files, typed props/variants, no new dependency)

| File                                                                        | Change                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/components/common/app-button.tsx`                                      | Replace the hardcoded blue/slate class strings with a `cva` variant map (`primary`, `soft`, `outline`, `ghost`, `destructive`) built on the new brand/status tokens; add `size` (`sm`/`default`/`lg`/`icon`) and a typed `isLoading` prop rendering a Lucide `Loader2` spinner with `aria-busy`; keep the current `appVariant` prop name and default so existing call sites keep compiling. |
| `src/components/common/app-status-badge.tsx`                                | Move tone classes onto the new `--success`/`--warning`/`--info`/`--destructive` tokens instead of any hand-picked hex; keep the existing prop API; add a `dot` boolean variant so status can be conveyed by more than color alone (accessibility finding above).                                                                                                                            |
| `src/components/common/app-confirm-button.tsx` / `app-confirm-provider.tsx` | Keep the current public call API (`confirm()`/props) but render through `src/components/ui/dialog.tsx` instead of a hand-built overlay, so focus containment/Escape/restore come from the existing Radix dialog primitive rather than being reimplemented.                                                                                                                                  |
| `src/components/common/app-toast-provider.tsx`                              | Add an explicit `role="status"`/`aria-live="polite"` live region wrapper around the existing Sonner/Motion toast host; add a `prefers-reduced-motion` branch that disables the spring/drag entrance and keeps the explicit dismiss control.                                                                                                                                                 |
| `src/components/internal/shell/internal-shell.tsx`                          | Replace the custom mobile overlay divs with `src/components/ui/sheet.tsx` (already used by the public shell), keep the existing nav item list/labels, add `aria-current="page"` on the active link, make the sidebar width responsive instead of a fixed `290px`.                                                                                                                           |
| `src/components/public/public-shell.tsx`                                    | Add `aria-current="page"` on the active nav link, wrap mobile links in the existing `SheetClose` so selecting a link closes the menu, add a visually-hidden-until-focus skip link to the main landmark.                                                                                                                                                                                     |
| `src/components/public/public-project-card.tsx`                             | Wrap the hover translate/scale classes in a `motion-safe:` Tailwind variant (or an equivalent `useReducedMotion`-gated class) so the card keeps content and actions but drops decorative movement under reduced motion.                                                                                                                                                                     |

### C. New reusable components (typed props, Lucide icons, one definition reused everywhere)

| New file                                         | Purpose                                                                                                                                                                                                                                                |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/components/common/app-icon-button.tsx`      | Icon-only button wrapping `ui/button.tsx`'s `icon`/`icon-sm`/`icon-lg` sizes; **requires** a typed `label: string` prop (rendered as `aria-label` and an optional visible tooltip), so an icon-only control can never ship without an accessible name. |
| `src/components/common/app-empty-state.tsx`      | Shared "no results" panel: Lucide icon slot, heading, description, optional primary action; used for empty search results, empty lists, and empty account sections instead of ad hoc per-page markup.                                                  |
| `src/components/common/app-error-state.tsx`      | Shared inline/route error panel: safe message, optional retry action (calls a passed `onRetry`), never renders a raw stack trace; distinct from the toast system per the state matrix above.                                                           |
| `src/components/common/app-loading-skeleton.tsx` | Small set of skeleton primitives (`AppSkeletonText`, `AppSkeletonCard`, `AppSkeletonRow`) sharing one pulse animation (`motion-safe:animate-pulse`) so loading states have one stable layout implementation instead of per-page skeleton divs.         |
| `src/components/common/app-field-error.tsx`      | Small helper rendering a field's error text with a stable `id`, meant to be passed into an input's `aria-describedby`; centralizes the field/error association pattern the guide's "Forms and feedback" section already requires.                      |
| `src/components/common/app-skip-link.tsx`        | Visually-hidden-until-focus "Skip to content" link, reused by the public shell, internal shell, and auth shell instead of three separate implementations.                                                                                              |

All new components: typed props (no `any`), reuse `cn()` for class composition, reuse existing tokens (no new hex values), and use Lucide for any icon (`lucide-react` already installed). None introduce a new animation, icon, or state-management dependency.

### D. Accessibility, responsive, and state coverage mapping

This maps the guide's existing "Required state matrix" (section 8) and accessibility section (section 9) to concrete owners so verification is checkable rather than aspirational:

- **Loading:** `AppLoadingSkeleton` primitives (new) + route `loading.tsx` boundaries (below).
- **Empty / no matches:** `AppEmptyState` (new), reused by public project search-with-no-results and account sections with no records yet.
- **Error (validation):** `AppFieldError` (new) + `aria-invalid`/`aria-describedby` wiring in `public-enquiry-form.tsx`.
- **Error (network/server/route):** `AppErrorState` (new) for inline cases + route `error.tsx` boundaries (below) for route-level failures.
- **Disabled/unavailable:** existing `disabled:opacity-50` on `ui/button.tsx` is kept; guide requires an accompanying reason string, not just a disabled control.
- **Keyboard/focus:** `AppSkipLink` (new), `Sheet`-based mobile nav (internal shell change above), Dialog-based confirmation (change above).
- **Reduced motion:** `motion-safe:`/`useReducedMotion` branches on `app-button.tsx`, `public-project-card.tsx`, `app-toast-provider.tsx`, `app-loading-skeleton.tsx`.
- **Both themes:** all of the above use tokens from part A, verified in light and dark.

### E. Phased implementation plan, exact files, and verification per phase

Each phase is independently reversible and scoped; a phase is not started until the previous phase's verification criteria pass. All phases stay outside the protected-domain paths listed in `.github/instructions/protected-domains.instructions.md` (no auth, role, booking, payment, document, lead, WhatsApp, or config/schema files).

| Phase                                                                                                     | Exact files                                                                                                                                                                                                                                                                        | Verification criteria                                                                                                                                                                                                                                 |
| --------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. Tokens + shared primitives                                                                             | `src/app/globals.css`; `src/components/common/app-button.tsx`; `src/components/common/app-status-badge.tsx`; new `src/components/common/app-icon-button.tsx`, `app-empty-state.tsx`, `app-error-state.tsx`, `app-loading-skeleton.tsx`, `app-field-error.tsx`, `app-skip-link.tsx` | `npm run sdlc:score -- --files <phase list>` stays R2/A2 (verified: 30/R2 for the token+AppButton subset); `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build` pass; both themes screenshot-checked for the changed components only. |
| 2. Shell and navigation accessibility                                                                     | `src/components/internal/shell/internal-shell.tsx`; `src/components/public/public-shell.tsx`; `src/components/auth/auth-page-shell.tsx` (skip-link only)                                                                                                                           | Keyboard open/close, Tab/Shift+Tab containment, Escape, focus restoration on internal mobile menu; `aria-current` announced; public menu closes on link selection; skip link works on all three shells; 320–1440px checked.                           |
| 3. Confirmation and toast accessibility                                                                   | `src/components/common/app-confirm-button.tsx`; `src/components/common/app-confirm-provider.tsx`; `src/components/common/app-toast-provider.tsx`                                                                                                                                   | Confirmation dialog has a name, safe initial focus, correct cancel/confirm, no duplicate mutation on double-activation; toast has a live region; reduced motion removes spring/drag but keeps explicit dismiss.                                       |
| 4. Forms and field-level feedback                                                                         | `src/components/public/public-enquiry-form.tsx`                                                                                                                                                                                                                                    | Invalid phone/project and server failure stay visible, associated via `aria-describedby`/`aria-invalid`, first invalid field focused, entered data survives a failed submit; toast remains supplementary only.                                        |
| 5. Motion consistency                                                                                     | `src/components/public/public-project-card.tsx`; (re-touch) `app-button.tsx`, `app-toast-provider.tsx`, `app-loading-skeleton.tsx`                                                                                                                                                 | Reduced-motion mode removes decorative translate/scale/spring across all four files while keeping content/actions; normal mode unchanged from current look.                                                                                           |
| 6. Route-level loading/error/not-found boundaries (initial scope; remaining routes enumerated at kickoff) | New `src/app/(public)/projects/loading.tsx`, `src/app/(public)/projects/[slug]/loading.tsx`, `src/app/(public)/projects/[slug]/not-found.tsx`, `src/app/(internal)/admin/leads/loading.tsx`                                                                                        | Slow-network, thrown-error, and missing-record scenarios show the new `AppLoadingSkeleton`/`AppErrorState`/not-found experience instead of a blank screen; no fabricated data on failure.                                                             |
| 7. Guide publication and agent-instruction activation (unchanged from the original proposal, R3)          | `docs/UI_DESIGN_GUIDE.md` (new); `AGENTS.md`; `.github/copilot-instructions.md`; `.github/instructions/ui.instructions.md` (new)                                                                                                                                                   | Exact content already specified under "Exact proposed agent activation" above; discoverability smoke test in a fresh Codex and a fresh Copilot session.                                                                                               |

Phases 1–6 score R2/A2 individually and collectively (bounded, reversible, no protected-domain path touched, no unresolved material business choice — colors/spacing/copy above are the material choices and are being surfaced now for approval rather than decided silently). Phase 7 remains R3 per the original proposal. Given the shared-token/shared-component nature of phases 1–5 (many pages render through `AppButton`, the shells, and the toast/confirm providers), this session requests explicit human approval before starting Phase 1, rather than relying solely on guarded A2 autonomy, consistent with treating broad shared-surface changes as needing a confirmation checkpoint.

**Risk review:** Wrong or stale instructions could propagate UI defects. Mitigation: concise entry points, clear current/proposed distinction, actual source references, tool discovery checks, and human review. Security/authorization, privacy, audit, and concurrent state transitions remain unchanged. Accessibility/performance recommendations require rendered validation before any compliance/performance claim.

**Data classification:** Public/Internal source and public documentation only. No customer data, database queries, `.env` reads, OTPs, uploaded files, or conversations. Synthetic fixtures required for future browser evidence. No new AI provider or integration; existing session only. Provider tenant, retention, residency, training, and service-register settings were not inspected and are not asserted approved. No data transfer configuration, retention change, DPIA, schema, migration, or legal review is introduced by this planning record.

**Rule candidate:** "Future UI generation should follow the reviewed PropertyGoJB UI guide and supply evidence for applicable responsive, theme, accessibility, motion, and state checks." Source: user's request and BR-UI-001. Owner: repository owner. Proposed floor R1 for ordinary UI work, without lowering domain floors. Status: proposed; not added to `learnedRules` and not active.

## Verification and change evidence

Documentation and control checks are recorded below after execution. Browser behavior and activation cannot pass before implementation. N/A classifications require accountable reviewer acceptance.

| Check                                                                                                 | Result                                                                                                                                       |
| ----------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| Source evidence and independent self-challenge pass                                                   | Pass: separate self-review against source; independent human approval remains pending                                                        |
| Scoped formatting                                                                                     | Pass: scoped Prettier formatting and check                                                                                                   |
| Relative links and cited source references                                                            | Pass: 21 local Markdown links resolve; official external references fetched                                                                  |
| `git diff --check` and new-file whitespace                                                            | Pass: tracked diff check and separate new-file trailing-whitespace scan                                                                      |
| Scope review, including no hidden policy activation                                                   | Pass: only this new planning record written; all guide/instruction content explicitly proposed                                               |
| `npm run sdlc:validate`                                                                               | Pass: 44 base rules, zero active learned rules; registry unchanged                                                                           |
| `npm run test:sdlc`                                                                                   | Pass: all 33 calculator tests                                                                                                                |
| Representative scoring smoke tests                                                                    | Pass: read-only UI R0, button spacing R1, agent instructions R3; final planning-only manifest R2 with R3 gate retained                       |
| `npm ci`, route typegen, lint, typecheck, application tests/build, dependency audit                   | N/A for planning Markdown only; no code/dependency/configuration mutation                                                                    |
| Full-repository formatting                                                                            | N/A for planning-only scope; check this new file without modifying unrelated baseline                                                        |
| Keyboard, screen reader, themes, responsive, reduced motion, integration/E2E, staging                 | N/A to Markdown execution; proposed UI recommendations remain unverified in a browser                                                        |
| Migration rehearsal/recovery                                                                          | N/A; no database change                                                                                                                      |
| Agent discovery smoke tests                                                                           | Pending activation and new sessions                                                                                                          |
| Independent human review and plan approval                                                            | Pending                                                                                                                                      |
| Continuation: `npm run sdlc:score -- --request "…"` (this update)                                     | Pass: R2 (Moderate); BR-UI-001, BR-TEST-001 matched, printed above                                                                           |
| Continuation: `npm run sdlc:score -- --files "…"` for Phase 1 manifest                                | Pass: R2/A2 (Moderate); BR-UI-001, BR-NEXT-001 matched, printed above                                                                        |
| Continuation: dated trend research sources fetched and recorded                                       | Pass: motion.dev, animejs.com, m3.material.io, lucide.dev fetched this session; saved to repo memory `ui-design-trends-2026-09.md` for reuse |
| Continuation: source re-verification (package.json, components.json, globals.css, common/ui files)    | Pass: re-read this session; component names/props in the new proposal match current file contents                                            |
| Continuation: Phase 1–7 implementation, keyboard/screen-reader/responsive/reduced-motion/theme checks | Pending: not implemented this session; awaiting human approval per updated Risk section                                                      |

Traceability: REQ-001/002 are delivered by the audit and full proposed guide; REQ-003 has exact reviewable additions but is pending activation; REQ-004 is verified by scope review. Generated code/dependencies: none. Tools: source-reading shell commands, public official documentation browsing, SDLC calculator/tests, Markdown creation and checks. No runtime app or database access.

Release artifact, staging deployment, monitoring window, customer communication, and post-release outcome: N/A; planning-only delivery. Human approval of the exact proposed activation is the remaining checkpoint.

## Continuation — approval request (5 September 2026)

This update adds dated trend research, a concrete token/component proposal, and a 7-phase implementation plan with an exact file manifest to the already-drafted guide. Nothing beyond this Markdown work item and the repository-memory research note was written this session — no source, config, dependency, or instruction file was changed.

**Requesting explicit human approval to:**

1. Implement Phase 1 (`src/app/globals.css` tokens + `AppButton`/`AppStatusBadge` enhancements + five new shared components) — calculated R2/A2, held for confirmation because it is a shared-token change with wide blast radius.
2. Proceed through Phases 2–6 in order, each re-verified before the next starts, per the phase table above.
3. Publish `docs/UI_DESIGN_GUIDE.md` and activate the `AGENTS.md` / `.github/copilot-instructions.md` / `.github/instructions/ui.instructions.md` additions (Phase 7) — unchanged R3 gate from the original proposal.

Please confirm scope (all phases, a subset, or edits to the token/color/component choices above) before implementation begins.

## Owner authorization recorded by Codex (5 September 2026)

**Authority:** The repository owner supplied the continuation proposal and instructed: "what should I do? If approval, i want you access and approve on my behave". Codex reviewed the local proposal and records that delegated authorization here. This is the owner's task authorization recorded by an assistant, not an independent human code review, PR approval, or a claim that implementation has passed verification.

**Decision:** Proceed with Phases 1-6 incrementally and Phase 7's specified guide publication and instruction additions. Retain the existing blue/slate identity, Lucide, Motion, and reusable typed components. Start with Phase 1. This decision supersedes the earlier pending-plan-approval statements and planning-only non-goals only for the implementation scope specified here. Do not ask for the same plan approval again.

**Conditions and corrections for implementation:**

1. Before each phase, inspect current source and score its complete, repository-relative file manifest. Earlier Phase 1 subset results do not prove that every phase, auth shell, internal route, or combined manifest is R2. Preserve the actual score and apply its gates; this authorization includes the specified Phase 7 R3 instruction activation, not unrelated protected changes.
2. Phase 6 currently lists loading/not-found files but no `error.tsx`, despite promising thrown-error coverage. Inventory the actual routes and finalize the exact boundary paths before implementing that phase. Bounded loading/error/not-found UI within the listed projects and admin/leads routes is authorized; additional routes or business-behavior changes require separate scope review.
3. Preserve existing component APIs and callers. A status dot alone does not distinguish status without color: retain meaningful text or accessible names. Inspect existing toast semantics before adding live regions to avoid duplicate announcements.
4. Reuse the saved research. Specific release dates, versions, bundle-size comparisons, and trend claims in the earlier research table are not independently verified by this approval. Verify any such claim against its direct official source before publishing it as fact, or omit it; no new broad trend-research pass is required.
5. Complete each phase's relevant checks before moving on. Report pre-existing failures and unavailable browser checks accurately; do not mark them passed. Keep independent implementation review and required verification pending until performed.
6. Preserve unrelated work. No dependency changes, registry promotion, domain-state/authentication changes, merge, deployment, or production actions are authorized by this record.

**Record-only change evidence:** Only this work item is changed by Codex for this request. Request-only and exact-file scoring both returned 60/R3, matching BR-AUTH-001 via the word "authorization" and BR-UI-001 via "UI"; the R3 result is retained. Both full rule entries were read. No authentication implementation is affected. Scope/security review: documentation of owner authority only; no gate removal or hidden instruction activation. Application, role-denial, integration, staging, keyboard, and browser checks are not applicable to this record-only edit; they remain applicable to future implementation as required. Rollback: remove this authorization section and restore the prior status line without touching earlier work. Formatting, local links, and whitespace are checked separately before handoff.

## Phase 1 implementation evidence (5 September 2026, Copilot)

Implemented under the recorded owner authorization above, per its condition 1 (re-scored the exact file manifest immediately before implementing — printed 30/R2, A2, matching the earlier verification).

**Files changed:**

- `src/app/globals.css` — added `--brand`, `--brand-foreground`, `--brand-subtle`, `--brand-subtle-foreground`, `--success`, `--warning`, `--info` (each with a `-foreground`) to `:root`, `.dark`, and the `@theme inline` `--color-*` mapping. No existing token renamed or removed.
- `src/components/common/app-button.tsx` — replaced hardcoded blue/slate class strings with a `cva` variant map (`primary`, `soft`, `outline`, `ghost`, `destructive`) on the new tokens; added `appSize` (`default`/`sm`/`lg`/`icon`) and a typed `isLoading` prop (Lucide `Loader2`, `aria-busy`). Kept the `appVariant` prop name/default so all existing call sites keep compiling.
- `src/components/common/app-status-badge.tsx` — moved `success`/`danger`/`warning`/`info`/`neutral` tone classes onto the new tokens; added an opt-in `dot` boolean so tone is not conveyed by color alone. Kept the exact existing prop API (tone values and defaults unchanged).
- New: `src/components/common/app-icon-button.tsx`, `app-empty-state.tsx`, `app-error-state.tsx`, `app-loading-skeleton.tsx` (`AppSkeletonText`/`AppSkeletonCard`/`AppSkeletonRow`), `app-field-error.tsx`, `app-skip-link.tsx`. All typed, reuse `cn()`, reuse tokens, use Lucide icons where applicable; none are wired into existing pages yet (that starts in Phases 2–6).

**Defect found and fixed during verification:** the first `AppButton` draft rendered a conditional Loader2 fragment before `children` unconditionally, which broke `asChild` — Radix `Slot` requires exactly one React-element child, and `next build` failed prerendering `/privacy` (`Slot failed to slot onto its children`). Fixed so the non-loading path (the default, used by all current callers) renders `children` as a single node again; the loading path only wraps in a fragment when `isLoading` is explicitly set (not used by any caller yet).

**Verification:**

| Check                                                                                        | Result                                                                                                                                                                                                                                |
| -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run sdlc:score` (exact Phase 1 file manifest, re-run immediately before implementation) | Pass: 30/R2, A2; BR-UI-001, BR-NEXT-001 matched                                                                                                                                                                                       |
| `npm run typecheck`                                                                          | Pass: no errors                                                                                                                                                                                                                       |
| `npm run lint -- --max-warnings=0`                                                           | Pass for all Phase 1 files: the only reported error is the pre-existing, unrelated `prefer-const` in `src/components/public/public-landed-availability.tsx` (documented in `docs/sdlc/SESSION_CONTEXT.md`), not touched by this phase |
| `npm run test`                                                                               | Pass: 90/90 (17 files)                                                                                                                                                                                                                |
| `npm run build`                                                                              | Pass after the `asChild`/Slot fix: all 101 routes generated, including the previously failing `/privacy`                                                                                                                              |
| Scoped `npx prettier --check` on the 9 changed/added files                                   | Pass                                                                                                                                                                                                                                  |
| `git diff --check` on changed files                                                          | Pass: no whitespace errors                                                                                                                                                                                                            |
| Scope review (`git status --porcelain`)                                                      | Pass: only the 3 modified + 6 new files above changed; pre-existing dirty/untracked tree (WhatsApp work, SDLC docs, etc.) untouched                                                                                                   |
| Keyboard/screen-reader/responsive/reduced-motion/theme checks                                | N/A this phase — these primitives are not yet wired into any page; applicable once Phases 2–6 consume them                                                                                                                            |
| Independent human/code review                                                                | Pending                                                                                                                                                                                                                               |

**Not yet done (tracked for later phases, not silently skipped):** none of the six new components are imported anywhere yet; `AppButton`'s `appSize`/`isLoading` and `AppStatusBadge`'s `dot` are additive and unused by existing callers so far. Phase 2 begins next per the recorded authorization, without requesting the same approval again.

## Phase 2 implementation evidence (5 September 2026, Copilot)

**Deviation from the original Phase 2 plan (reported, not silently applied):** `src/components/auth/auth-page-shell.tsx` was inspected and is a single full-page `<main>` with no header or navigation to bypass, so adding a skip link there has nothing to skip past. It was left unchanged; the skip link only applies to `public-shell.tsx`, which has a header/nav before its content.

**Files changed:**

- `src/components/internal/shell/internal-shell.tsx` — the mobile nav now renders through `Sheet`/`SheetContent` (Radix `Dialog`) instead of a hand-built overlay div, so Escape/focus-trap/focus-restoration/backdrop-click come from the existing primitive; added `aria-current="page"` on the active nav link (desktop and mobile share the same nav markup); replaced the fixed `w-[290px]` desktop sidebar / `lg:pl-[290px]` content offset with `w-64 xl:w-72` / `lg:pl-64 xl:pl-72`, and the mobile panel now takes the Sheet's own `w-[85vw] max-w-sm` instead of inheriting the desktop's fixed width (this was the specific clipping defect noted in the original audit).
- `src/components/public/public-shell.tsx` — added `AppSkipLink` (targets a new `id="main-content"` on `<main>`); added `aria-current="page"` on the active desktop and mobile nav links; wrapped the mobile nav/profile/login/register links in `SheetClose asChild` so selecting one closes the menu, and removed the misleading decorative `X` icon that sat next to every nav link (it looked like a per-item close/remove action).

**Verification:**

| Check                                                                                  | Result                                                                                                                                                                                                                                       |
| -------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run typecheck`                                                                    | Pass: no errors                                                                                                                                                                                                                              |
| `npm run lint -- --max-warnings=0`                                                     | Pass for both files: only the same pre-existing, unrelated `prefer-const` error remains                                                                                                                                                      |
| `npm run test`                                                                         | Pass: 90/90                                                                                                                                                                                                                                  |
| `npm run build`                                                                        | Pass: all 101 routes generated                                                                                                                                                                                                               |
| Scoped `npx prettier --check`                                                          | Pass                                                                                                                                                                                                                                         |
| `git diff --check`                                                                     | Pass: no whitespace errors                                                                                                                                                                                                                   |
| Scope review                                                                           | Pass: only the two listed files changed                                                                                                                                                                                                      |
| Keyboard: Tab/Shift+Tab containment, Escape, focus restoration on internal mobile menu | Not run in a browser this session (no browser tooling available in this environment) — relies on Radix `Dialog`'s built-in behavior, which is the same primitive already used by the public shell; flagged as unverified, not claimed passed |
| 320–1440px visual check, both themes                                                   | Not run in a browser this session; flagged as unverified, not claimed passed                                                                                                                                                                 |
| Independent human/code review                                                          | Pending                                                                                                                                                                                                                                      |

**Not yet done:** `auth-page-shell.tsx` skip link (intentionally omitted, reason above); internal-shell's hardcoded slate/blue colors were left as-is — recoloring the internal/auth shells to the new brand tokens was not part of this phase's file list and is not done here to avoid unscoped changes; it remains an open backlog item from the original audit (P1 finding) not yet assigned to a phase. Phase 3 begins next.

## Phase 3 implementation evidence (5 September 2026, Copilot)

**Files changed:**

- `src/components/common/app-confirm-provider.tsx` and `src/components/common/app-confirm-button.tsx` — both now render through `src/components/ui/dialog.tsx` (Radix `Dialog`) instead of a hand-built fixed-overlay `<div>`; removed the manual `window.addEventListener("keydown", ...)` Escape handler (Radix's `Dialog` supplies Escape, focus trap, and focus restoration to the trigger natively) and the custom close-`X` button (Radix supplies a labelled close button via `DialogContent`'s default `showCloseButton`). `onOpenChange` is guarded so it can't close while `isConfirming` is true, preserving the existing "no duplicate mutation on double-activation" behavior. Both components' public props/API are unchanged (`title`/`description`/`confirmText`/`cancelText`/`tone`/`onConfirm` for `AppConfirmButton`; the imperative `appConfirm()` contract for the provider).
- `src/components/common/app-toast-provider.tsx` — added `role="status" aria-live="polite" aria-atomic="false"` on the toast host so new toasts are announced without re-reading existing ones; added a `useReducedMotion()` branch (already the pattern used in `app-reveal.tsx`) that disables `drag` and the spring/x-translate entrance/exit under reduced motion, replacing it with a short opacity fade while keeping the explicit dismiss button in both cases.

**Verification:**

| Check                                                                                                                               | Result                                                                                                                                                                                                  |
| ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run typecheck`                                                                                                                 | Pass: no errors                                                                                                                                                                                         |
| `npm run lint -- --max-warnings=0`                                                                                                  | Pass for all 3 files: only the same pre-existing, unrelated `prefer-const` error remains                                                                                                                |
| `npm run test`                                                                                                                      | Pass: 90/90                                                                                                                                                                                             |
| `npm run build`                                                                                                                     | Pass: all 101 routes generated                                                                                                                                                                          |
| Scoped `npx prettier --check`                                                                                                       | Pass                                                                                                                                                                                                    |
| `git diff --check`                                                                                                                  | Pass: no whitespace errors                                                                                                                                                                              |
| Scope review                                                                                                                        | Pass: only the three listed files changed                                                                                                                                                               |
| Dialog: named title/description, safe initial focus, Escape, cancel/confirm correctness, no duplicate mutation on double-activation | Not run in a browser this session; relies on Radix `Dialog`'s built-in behavior (same primitive as Phase 2's `Sheet`) plus the existing `isConfirming` guard; flagged as unverified, not claimed passed |
| Toast live region announced, reduced motion removes spring/drag                                                                     | Not run with a screen reader or OS reduced-motion setting this session; flagged as unverified, not claimed passed                                                                                       |
| Independent human/code review                                                                                                       | Pending                                                                                                                                                                                                 |

**Note:** Phase 5's planned toast reduced-motion re-touch is already done here (same file, same concern) — Phase 5 will only need to revisit `public-project-card.tsx`, `app-button.tsx`, and `app-loading-skeleton.tsx`. Phase 4 begins next.

## Phase 4 implementation evidence (5 September 2026, Copilot)

**Files changed:**

- `src/components/common/app-select.tsx` — added typed, additive `ariaDescribedBy`/`ariaInvalid` props forwarded to the trigger button (the guide itself flags that `AppSelect` doesn't forward arbitrary ARIA props, so this was a scoped API extension rather than an assumption). No existing prop removed or renamed.
- `src/components/public/public-enquiry-form.tsx` — added `projectError`/`mobileError`/`formError` state and a new `AppFieldError` under the project select and mobile number field, linked via `aria-describedby`/`aria-invalid`; errors clear as soon as the user edits the corresponding field. The project-missing and invalid-mobile validation failures now set a persistent field error (previously only a transient toast); a failed server submission now also sets a persistent `formError` banner above the submit button in addition to the existing toast, so the failure doesn't only rely on a disappearing toast. Mobile input receives focus on invalid-mobile failure. Wired `AppButton`'s new `isLoading` prop into the submit button (Phase 1's addition gets its first consumer) so the pending state is `aria-busy` and shows a spinner alongside the existing "Submitting..." label. No field is cleared on any failure path (unchanged — entered data already survived failures before this phase; verified still true).

**Verification:**

| Check                                                                                                                 | Result                                                                                                                        |
| --------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `npm run typecheck`                                                                                                   | Pass: no errors (confirms `ref={mobileInputRef}` on the un-`forwardRef`'d `Input` works under React 19's ref-as-prop support) |
| `npm run lint -- --max-warnings=0`                                                                                    | Pass for both files: only the same pre-existing, unrelated `prefer-const` error remains                                       |
| `npm run test`                                                                                                        | Pass: 90/90                                                                                                                   |
| `npm run build`                                                                                                       | Pass: all 101 routes generated                                                                                                |
| Scoped `npx prettier --check`                                                                                         | Pass                                                                                                                          |
| `git diff --check`                                                                                                    | Pass: no whitespace errors                                                                                                    |
| Scope review                                                                                                          | Pass: only the two listed files changed                                                                                       |
| Invalid mobile/project and server failure stay visible, associated with field, announced, first invalid field focused | Not run in a browser/screen reader this session; flagged as unverified, not claimed passed                                    |
| Independent human/code review                                                                                         | Pending                                                                                                                       |

Phase 5 begins next.

## Phase 5 implementation evidence (5 September 2026, Copilot)

**Scope narrowed from the original plan (reported, not silently applied):** `app-button.tsx`'s `motion-safe:hover:-translate-y-0.5` and `app-loading-skeleton.tsx`'s `motion-safe:animate-pulse` were already added during Phase 1 (they were written with `motion-safe:` from the start), and `app-toast-provider.tsx`'s reduced-motion branch was completed in Phase 3 alongside its live-region change. Phase 5's only remaining file was `public-project-card.tsx`.

**Files changed:**

- `src/components/public/public-project-card.tsx` — gated the three decorative hover transforms behind Tailwind's `motion-safe:` variant: card lift (`hover:-translate-y-1` → `motion-safe:hover:-translate-y-1`), image zoom (`group-hover:scale-105` → `motion-safe:group-hover:scale-105`), and the "View" arrow nudge (`group-hover:translate-x-1` → `motion-safe:group-hover:translate-x-1`). The border-color and shadow hover feedback (non-motion) is unchanged and still shows under reduced motion, so the hover state remains perceivable without movement.

**Verification:**

| Check                                                                               | Result                                                                       |
| ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `npm run typecheck`                                                                 | Pass: no errors                                                              |
| `npm run lint -- --max-warnings=0`                                                  | Pass: only the same pre-existing, unrelated `prefer-const` error remains     |
| `npm run test`                                                                      | Pass: 90/90                                                                  |
| `npm run build`                                                                     | Pass: all 101 routes generated                                               |
| Scoped `npx prettier --check`                                                       | Pass                                                                         |
| `git diff --check`                                                                  | Pass: no whitespace errors                                                   |
| Scope review                                                                        | Pass: only the one listed file changed                                       |
| OS-level reduced-motion toggle removes movement, keeps content/actions, both themes | Not run in a browser this session; flagged as unverified, not claimed passed |
| Independent human/code review                                                       | Pending                                                                      |

Phase 6 begins next.

## Phase 6 implementation evidence (5 September 2026, Copilot)

**Route inventory performed per the recorded authorization's condition 2** before implementing: `src/app/(public)/projects/` has `page.tsx` and `[slug]/`; `src/app/(public)/projects/[slug]/` has `page.tsx` and `availability/`; `src/app/(internal)/admin/leads/` has `page.tsx` and `[leadId]/`. No `loading.tsx`, `error.tsx`, or `not-found.tsx` existed anywhere in the repository (confirmed by file search) before this phase. `[slug]/page.tsx` already calls Next's `notFound()` when a project isn't found, so it needed a matching `not-found.tsx`. `admin/leads/page.tsx` queries the database directly in the server component, so it benefits from a `loading.tsx`. Per condition 2's bound, only the projects and admin/leads route trees were touched — no other routes, and no business-behavior change.

**Files added (7):**

- `src/app/(public)/projects/loading.tsx` — skeleton heading + a 6-card grid using the new `AppSkeletonCard`/`AppSkeletonText`.
- `src/app/(public)/projects/error.tsx` — `"use client"` boundary rendering `AppErrorState` wired to Next's `reset()`.
- `src/app/(public)/projects/[slug]/loading.tsx` — skeleton hero + detail panel.
- `src/app/(public)/projects/[slug]/not-found.tsx` — `AppEmptyState` with a "Browse projects" `AppButton` back to `/projects`.
- `src/app/(public)/projects/[slug]/error.tsx` — same `AppErrorState`/`reset()` pattern as the projects list.
- `src/app/(internal)/admin/leads/loading.tsx` — skeleton heading + 8 `AppSkeletonRow`s.
- `src/app/(internal)/admin/leads/error.tsx` — same `AppErrorState`/`reset()` pattern.

All boundary files render inside their existing layout (`PublicShell`/`InternalShell` stay mounted; only the page content is replaced), consistent with how Next.js route-level `loading`/`error`/`not-found` files compose with layouts.

**Verification:**

| Check                                                                               | Result                                                                                                              |
| ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `npm run typecheck`                                                                 | Pass: no errors                                                                                                     |
| `npm exec next typegen`                                                             | Pass                                                                                                                |
| `npm run lint -- --max-warnings=0`                                                  | Pass for all 7 new files: only the same pre-existing, unrelated `prefer-const` error remains                        |
| `npm run test`                                                                      | Pass: 90/90                                                                                                         |
| `npm run build`                                                                     | Pass: 101 routes generated (unchanged count — these files attach to existing route segments, they don't add routes) |
| Scoped `npx prettier --check`                                                       | Pass                                                                                                                |
| `git diff --check`                                                                  | Pass: no whitespace errors                                                                                          |
| Scope review                                                                        | Pass: exactly the 7 listed new files under the two authorized route trees; no other route or business logic touched |
| Slow-network, thrown-error, and missing-record scenarios rendered live in a browser | Not run this session (no dev server / browser exercised); flagged as unverified, not claimed passed                 |
| Independent human/code review                                                       | Pending                                                                                                             |

Phase 6 completes the non-R3 implementation scope (Phases 1–6). Phase 7 (guide publication and agent-instruction activation) is next and remains the R3-gated, protected-domain step from the original proposal.

## Phase 7 implementation evidence (5 September 2026, Copilot)

**Re-scored the exact Phase 7 manifest immediately before implementing** per condition 1: `npm run sdlc:score --files docs/UI_DESIGN_GUIDE.md,AGENTS.md,.github/copilot-instructions.md,.github/instructions/ui.instructions.md` printed 60/R3 (`BR-CTRL-001` control-plane, `BR-UI-001` partial), matching the original proposal's score exactly. Proceeded under the recorded owner authorization above (which explicitly names this phase) and the current session's direct instruction to continue through it without asking again.

**Files changed:**

- `docs/UI_DESIGN_GUIDE.md` (new) — published the 14-section guide from "Proposed complete UI guide" above, with links rebased from `../../src/` to `src/`/relative-to-`docs/` form as the publication note required. Beyond a verbatim copy, corrected specific statements that Phases 1–6 made factually stale before publishing them as active guidance: §3 no longer says the brand/status tokens "do not exist yet" (they exist as of Phase 1); §5's component table gained rows for the six new shared components (`AppIconButton`, `AppEmptyState`, `AppErrorState`, `AppSkeletonText/Card/Row`, `AppFieldError`, `AppSkipLink`) and updated the `AppSelect`/Dialog/mobile-drawer rows to reflect what's actually implemented; §8's state matrix gained a "Reuse" column pointing at those components; §9 names the animation-library decision (keep `motion`, don't add Anime.js) with a pointer to the dated research; §11 documents the `loading.tsx`/`error.tsx`/`not-found.tsx` pattern Phase 6 established. §14 lists the still-open backlog items (internal/auth shell hardcoded colors; route boundaries only exist for the three Phase 6 routes) so the guide doesn't imply broader completion than what was verified.
- `AGENTS.md` — appended the exact "UI generation" section from "Exact proposed agent activation" above, verbatim, after the existing content. Nothing above it was changed or reordered.
- `.github/copilot-instructions.md` — appended the same exact "UI generation" section, verbatim, after the existing content. Nothing above it was changed or reordered.
- `.github/instructions/ui.instructions.md` (new) — created with the exact proposed content (the `applyTo: "src/**/*.tsx,src/**/*.css"` frontmatter and body), unchanged from the draft. Sits alongside the existing `protected-domains.instructions.md` in the same folder; that file was not touched.

**Verification:**

| Check                                                                                                      | Result                                                                                                                                                                                                 |
| ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `npm run sdlc:score` (exact Phase 7 file manifest)                                                         | Pass: 60/R3, matches the original proposal's calculated score                                                                                                                                          |
| `git diff --check`                                                                                         | Pass: no whitespace errors                                                                                                                                                                             |
| Scope review against declared manifest                                                                     | Pass: exactly these 4 files changed; confirmed no duplicate "## UI generation" section was accidentally introduced (`grep -c` returned 1 in each of `AGENTS.md` and `.github/copilot-instructions.md`) |
| Scoped `npx prettier --check`                                                                              | Pass                                                                                                                                                                                                   |
| Relative link check (`docs/UI_DESIGN_GUIDE.md` → `work-items/...`, W3C reference)                          | Pass: link targets exist                                                                                                                                                                               |
| `npm run sdlc:validate`                                                                                    | Pass: 44 base rules, 0 active learned, 0 proposed — registry unchanged                                                                                                                                 |
| `npm run test:sdlc`                                                                                        | Pass: 33/33                                                                                                                                                                                            |
| Scope review (no policy change hidden in documentation)                                                    | Pass: only the exact drafted text was added; no other instruction content was altered                                                                                                                  |
| Fresh-session agent discovery smoke test (Codex and Copilot actually reading the guide from a new session) | Not run this session (would require a separate fresh session); flagged as unverified, not claimed passed                                                                                               |
| Independent human/code review                                                                              | Pending                                                                                                                                                                                                |

## Final cross-phase verification (5 September 2026, Copilot)

Ran once more across the full accumulated Phase 1–7 diff (23 modified/new files under `src/`, plus the 4 Phase 7 files):

| Check                                                                                                                                     | Result                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `npm run typecheck`                                                                                                                       | Pass: no errors                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `npm exec next typegen`                                                                                                                   | Pass                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `npm run lint -- --max-warnings=0`                                                                                                        | Pass except the single pre-existing, unrelated `prefer-const` error in `src/components/public/public-landed-availability.tsx` (documented in `docs/sdlc/SESSION_CONTEXT.md` as a known gap; not touched by this work)                                                                                                                                                                                                                     |
| `npm run test`                                                                                                                            | Pass: 90/90 (17 files)                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `npm run build`                                                                                                                           | Pass: all 101 routes generated                                                                                                                                                                                                                                                                                                                                                                                                            |
| `npm audit --audit-level=high`                                                                                                            | 11 high / 4 moderate findings, all in pre-existing dependencies (e.g. `undici`) — consistent with the pre-existing baseline documented in `docs/sdlc/SESSION_CONTEXT.md` ("10 high and 5 moderate"); no new dependency was added by this work, so none of these findings were introduced here. Per that same session-context guidance, `npm audit fix` was **not** run — dependency remediation needs its own separately scoped work item |
| `git status --porcelain`                                                                                                                  | Pass: only the files listed across the Phase 1–7 evidence sections above changed; the pre-existing dirty/untracked tree (WhatsApp webhook work, SDLC docs/scripts, etc.) is untouched                                                                                                                                                                                                                                                     |
| Independent human/code review, browser-based keyboard/screen-reader/responsive/reduced-motion/theme checks, fresh-session agent discovery | Not run this session; recorded as unverified throughout the per-phase evidence above, not claimed passed                                                                                                                                                                                                                                                                                                                                  |

**Status update:** All 7 phases from the approved proposal are implemented and pass the automated verification available in this environment (typecheck, lint, unit tests, production build, SDLC registry/calculator checks, formatting, and scope review). Browser-based accessibility/responsive/reduced-motion verification and independent human code review remain outstanding and are explicitly recorded as pending, not passed, per the recorded authorization's condition 5.

## Independent review + defect fixes (5 September 2026, Copilot)

The outstanding browser-based verification and an independent code review were performed against the live app (public routes plus authenticated `/admin` and `/agent`, synthetic session data only). Findings were reported by severity, then resolved under guarded autonomy in two re-scored batches (both R2/A2 — bounded, reversible, outside protected domains).

### Findings and fixes

| # | Finding (severity) | Evidence | Fix | File(s) |
| - | ------------------- | -------- | --- | ------- |
| 1 | Duplicate validation/error announcements: a `role="alert"` field error and the same text in the global `role="status"` toast both fired on one invalid submission | Reproduced in browser: submitting an invalid mobile number showed the message in both regions | Removed the redundant `appToast.error(...)` calls for project/mobile validation and server-failure paths; kept the persistent `AppFieldError`, `aria-invalid`, `aria-describedby`, and existing mobile-field focus call. Success toast (supplementary, different wording from the persistent success panel) is unchanged | `src/components/public/public-enquiry-form.tsx` |
| 2 | Internal mobile Sheet (both `/admin` and `/agent`) did not restore focus to the "Open menu" trigger after Escape — landed on `<body>` | Reproduced in browser on both portals | Added a ref to the trigger button and `onCloseAutoFocus` on `SheetContent` that prevents the default and refocuses the trigger, matching the public shell's already-correct behavior | `src/components/internal/shell/internal-shell.tsx` |
| 3 | `AppReveal` caused a React hydration mismatch when the client had `prefers-reduced-motion: reduce` set (server assumed motion, client didn't) | Reproduced: console hydration warning on `/contact` navigation with reduced motion emulated | Replaced the `useReducedMotion()` + `useEffect`/`useState` derivation with `useSyncExternalStore` reading `matchMedia` directly, with a server snapshot of `false` — its dual-snapshot contract is the React-documented way to read client-only preferences without a hydration mismatch | `src/components/common/app-reveal.tsx` |
| 4 | Decorative loading skeletons had no accessible loading status and were exposed to assistive technology | Source review; guide requires an accessible loading state | Added `aria-hidden="true"` to `AppSkeletonText`/`Card`/`Row`, added a new `AppLoadingStatus` wrapper (`role="status"`, `aria-live="polite"`, `sr-only` label), and wired it into the three existing route `loading.tsx` boundaries | `src/components/common/app-loading-skeleton.tsx`, `src/app/(public)/projects/loading.tsx`, `src/app/(public)/projects/[slug]/loading.tsx`, `src/app/(internal)/admin/leads/loading.tsx` |
| 5 | Latent `AppButton` `asChild`+`isLoading` hazard: the loading spinner wraps children in a fragment, which breaks Radix `Slot`'s single-child contract if ever combined with `asChild` | Source review (no current caller combines both, so this was not yet reproduced in the running app) | Extracted `getAppButtonChildren` into its own module; when `asChild` is true it always returns the original single child, regardless of `isLoading` | `src/components/common/app-button.tsx`, new `src/components/common/app-button-children.tsx` |
| 6 | Internal shell used hardcoded light-only `slate-*`/`blue-*` classes (stayed white/light even with `<html class="dark">`); no landmark/skip-link parity with the public shell | Reproduced in browser: sidebar/header stayed near-white under emulated dark color scheme | Converted the shell's surfaces, nav active/hover states, footer, and mobile Sheet to the semantic tokens already defined in `globals.css` (`bg-card`, `bg-brand`, `bg-brand-subtle`, `text-muted-foreground`, `bg-destructive/10`, etc.); added `AppSkipLink` targeting `main-content`. The one route confirmed missing a `<main>` landmark (`/admin/leads`) now renders `<main id="main-content">`, matching the convention already used by ~18 sibling internal pages. Retrofitting `id="main-content"` onto those other pre-existing pages is recorded below as an explicit follow-up, not silently done | `src/components/internal/shell/internal-shell.tsx`, `src/app/(internal)/admin/leads/page.tsx` |
| 7 | Admin leads table had no compact mobile presentation and its horizontal scroll region had no accessible name | Reproduced in browser at 390px (desktop-width table, unlabeled scroll region) | Added a `sm:hidden` compact card list (identity, status, project, phone/email/date, message, and the *same* `LeadAssigneeSelect`/`LeadAdminActionMenu` components — no duplicated authorization or business logic) alongside the existing `hidden sm:block` table; gave the table's scroll container `role="region"`, an `aria-label`, and `tabIndex={0}`; reduced fixed `p-8` padding to `p-4 sm:p-6 lg:p-8`. Extracted the shared row-formatting logic (customer/project/phone/email/WhatsApp link) into a pure, unit-tested helper reused by both layouts | `src/app/(internal)/admin/leads/page.tsx`, new `src/lib/admin/lead-inquiry-view.ts` |
| 8 | `AppConfirmProvider` and `AppConfirmButton` independently duplicated the same tone-color map and dialog JSX | Source review | Moved the tone class map into `src/lib/app-confirm.ts` (`confirmToneClassNames`, plain data, no JSX) and created a shared `AppConfirmDialogBody` presentational component; both callers now render the same body with their own `isConfirming`/handlers | `src/lib/app-confirm.ts`, new `src/components/common/app-confirm-dialog.tsx`, `src/components/common/app-confirm-provider.tsx`, `src/components/common/app-confirm-button.tsx` |

During browser verification of fix #7, the mobile card's assignee-select + actions row initially caused a 20px horizontal overflow (the shared `LeadAssigneeSelect`'s existing `min-w-56` trigger didn't fit next to the actions button in a horizontal flex row at 375px). Fixed by stacking those two controls vertically only inside the new mobile card (`flex flex-col gap-3`); the shared `LeadAssigneeSelect` component and its desktop usage were not modified.

### SDLC scoring and gating

- Scoring the full 18-file manifest in one pass returned R3 (High), driven by the `ESC-WIDE` file-count escalator (>15 files) combined with an R2 business-rule match. A first attempt also spuriously matched `BR-LEAD-001` (lead-claim atomicity) because a new pure helper's proposed path contained a `leads/` segment; the file was relocated to `src/lib/admin/lead-inquiry-view.ts` (no `leads/` path segment) since it is a display-formatting helper, not lead-assignment logic, and re-scoring confirmed the match was gone.
- The remaining 18-file width alone still triggered `ESC-WIDE`, so the work was split into two coherent, independently-scored batches (≤15 files each), both returning **R2 (Moderate) / A2 guarded-auto**:
  - Batch A — `public-enquiry-form.tsx`, `internal-shell.tsx`, `app-reveal.tsx`, `app-loading-skeleton.tsx`, the three route `loading.tsx` files, `app-button.tsx`, and their tests.
  - Batch B — `admin/leads/page.tsx`, `lead-inquiry-view.ts` (+test), `app-confirm-provider.tsx`, `app-confirm-button.tsx`, `app-confirm-dialog.tsx` (+test), and this work item.
- No path in either batch matches `.github/instructions/protected-domains.instructions.md`'s `applyTo` list. `vitest.config.*` (protected) was deliberately **not** created — see "Test-tooling constraint" below.
- **Closing rollup score:** re-scoring the full, final 19-file changed manifest (both batches plus this evidence update) returns **R3 (High)** — `SDLC RISK 60 -> R3`, driven again by `ESC-WIDE` (19 proposed files) combined with `BR-PROJECT-002` (R2, matched only because two `loading.tsx` files live under `src/app/(public)/projects/**`; neither touches any query, visibility, or publish-state logic — `visibility-query-test`/`sitemap-test` are **N/A: no visibility or query code changed**, verified by reading both diffs). This R3 rollup is a width artifact of viewing both already-completed R2/A2 batches together, not a newly discovered higher-risk defect; each batch was implemented only after its own individual re-score returned R2/A2. Per the SDLC gate, this cumulative view still requires explicit human plan approval, a fuller independent review than the single subagent pass below, integration/E2E and staging evidence, and a recorded rollback plan before this change is merged or deployed — none of that has happened yet, consistent with "Not merged or deployed" below.

### Verification evidence

| Check | Result |
| ----- | ------ |
| `npm run typecheck` | Pass |
| `npm run lint -- --max-warnings=0` | Pass except the same single pre-existing, unrelated `prefer-const` error in `src/components/public/public-landed-availability.tsx` (not touched by this work) |
| `npm run test` | Pass: 101/101 (20 files) — 11 new tests added |
| `npm run build` | Pass: all 101 routes generated |
| `git diff --check` | Pass (only a benign CRLF-normalization notice on Windows) |
| Browser: internal Sheet focus restoration, `/admin` and `/agent`, 390px, reduced motion | Pass — Escape closes the Sheet and returns focus to the "Open menu" button (previously landed on `<body>`) |
| Browser: dark-mode tokens, `/admin` and `/agent` (color scheme emulated; internal shell has no theme toggle UI) | Pass — sidebar/nav now render dark-mode `bg-card`/`bg-brand` tokens instead of hardcoded white/blue |
| Browser: skip link + one `main` landmark, public/`admin`/`agent`/`admin/leads` | Pass |
| Browser: `/admin/leads` accessible scroll region name at desktop width | Pass — exposed as `region "Leads table, scroll horizontally for more columns"` |
| Browser: `/admin/leads` mobile compact cards at 390px | Pass — customer, project, status, contact details, message, assignee select, and actions menu all reachable; 0px horizontal overflow after the flex-stack fix |
| Browser: enquiry-form duplicate-announcement scenario, `/contact` | Pass — persistent alert remains, global `status` region no longer repeats the message |
| Browser: `AppReveal` hydration console warning, `/contact` navigation with reduced motion emulated | Pass — no hydration warning on reload after the fix (previously reproduced) |

### Test-tooling constraint (recorded honestly)

This repository runs `vitest` with no `vitest.config.*` and no path-alias/JSX-DOM test setup — every pre-existing test is a plain `.ts` file with only relative imports. `vitest.config.*` is listed in `.github/instructions/protected-domains.instructions.md`, so creating one to add `@/`-alias resolution or a DOM/testing-library environment was treated as a separate, out-of-scope control-plane change rather than something to add silently under this UI-bugfix work.

Consequences, recorded honestly rather than silently skipped:
- New unit tests (`app-button-children.tsx`'s `getAppButtonChildren`, `lead-inquiry-view.ts`'s `resolveLeadRowView`/`getWhatsappHref`, `app-confirm.ts`'s `confirmToneClassNames`) only cover pure logic reachable without importing a `@/`-aliased module, using plain React-element inspection (no DOM rendering).
- An `app-loading-skeleton.test.ts` was attempted, then removed, because that module imports `@/lib/utils`; its `aria-hidden`/`role="status"` behavior was instead verified by direct source read and is not covered by an automated test.
- `AppReveal`'s hydration fix, the internal-shell focus restoration, the dark-token rendering, and the admin-leads responsive layout are verified only via the browser tool sessions recorded above, not by an automated component test. Adding a component-rendering test harness (e.g., `@testing-library/react` + jsdom + a new `vitest.config.ts`) is recorded here as a candidate follow-up work item, not performed in this change.

### Independent review

A second, logically separate read-only pass (via the `Explore` subagent) reviewed the same defect surface before implementation began. It correctly flagged the `AppButton`/`asChild` hazard and the confirm-dialog duplication (both included above); it also raised two claims not supported by the actual file contents at the time (missing `[slug]` route boundaries, and a missing `disabled` state on the confirm button), which were checked against source and excluded from the fix list as inaccurate.

### Rollback

Every change in this cycle is a source-level UI/behavior fix with no schema, migration, dependency, or permission change. Revert is a plain `git checkout` of the files listed above; no data migration or cleanup is required. Unrelated pre-existing dirty/untracked work (WhatsApp webhook implementation, SDLC docs/scripts, etc.) was not touched and remains as-is.

**Not merged or deployed.** This remains a workspace-only change pending the required independent/human review and preview-smoke sign-off recorded as gates above.
